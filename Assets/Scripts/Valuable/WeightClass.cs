namespace Collectives.ValuableSystems
{
    // Each number represents 1% of player's base walk speed.
    // If the player's base walk speed is 100, then a value of 15 will reduce the player's speed by 15.
    public enum EWeightClasses
    {
        LIGHT = 0,
        MEDIUM = 20,
        HEAVY = 40,
    }
}